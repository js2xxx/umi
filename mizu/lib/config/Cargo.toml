[package]
edition = "2021"
license = "MIT OR Apache-2.0"
name = "config"
version = "0.1.0"

[features]
cv1811h = []
qemu-virt = []

[dependencies]
cfg-if = "1"
num-rational = {version = "0", default-features = false}
