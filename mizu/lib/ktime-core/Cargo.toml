[package]
edition = "2021"
license = "MIT OR Apache-2.0"
name = "ktime-core"
version = "0.1.0"

[features]
default = ["qemu-virt"]
qemu-virt = ["config/qemu-virt"]

[dependencies]
# Local crates
config = {path = "../config"}
# External crates
riscv = "0"
